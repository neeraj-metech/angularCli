import { TextFormatePipe } from './text-formate.pipe';

describe('TextFormatePipe', () => {
  it('create an instance', () => {
    const pipe = new TextFormatePipe();
    expect(pipe).toBeTruthy();
  });
});
